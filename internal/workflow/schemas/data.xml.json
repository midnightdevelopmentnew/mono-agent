{
  "credential_platform": null,
  "fields": [
    { "key": "operation", "label": "Operation", "type": "select", "required": true, "options": ["parse", "generate"], "default": "parse" },
    { "key": "field", "label": "XML Field", "type": "text", "required": true, "placeholder": "xml_content" },
    { "key": "output_field", "label": "Output Field", "type": "text", "required": false, "help": "Defaults to the XML field, overwriting it." },
    { "key": "root_element", "label": "Root Element Name", "type": "text", "required": false, "default": "root", "depends_on": { "key": "operation", "values": ["generate"] } }
  ]
}
