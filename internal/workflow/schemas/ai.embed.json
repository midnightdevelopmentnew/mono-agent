{
  "credential_platform": null,
  "fields": [
    { "key": "provider_id", "label": "AI Provider", "type": "text", "required": true, "help": "ID of a configured AI provider (see AI Providers settings)." },
    { "key": "model", "label": "Model", "type": "text", "required": false, "help": "Falls back to the provider's default model." },
    { "key": "input_field", "label": "Input Field", "type": "text", "required": false, "help": "Field containing text to embed. Defaults to the full item JSON." },
    { "key": "output_key", "label": "Output Key", "type": "text", "required": false, "default": "embedding" },
    { "key": "max_tokens", "label": "Max Tokens", "type": "number", "required": false, "default": 256 }
  ]
}
